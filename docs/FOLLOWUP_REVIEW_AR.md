# متابعة فحص التطبيق — 16 سبتمبر 2026

تعالج هذه المتابعة حالات أعيد إنتاجها في اختبارات منفصلة بعد تحسين مصادر البيانات والأداء.

| المشكلة | التصحيح |
| --- | --- |
| عند بطء RNT، كانت مهلة الانتظار تُسجّل لكل الأعضاء قبل أن يبدأ طلبهم، مما قد يحرم بعضهم من المحاولة | يُحجز كل لاعب عند وصول دوره فقط، وتُقدّم الطلبات التي لم تُجرّب أو مضى على تجربتها وقت أطول، مع الحفاظ على أربعة طلبات متزامنة وميزانية الثماني ثواني وحدود المصدر |
| استجابة لاعب ناقصة قد تستبدل إحصاءات حساب محفوظة بقيم فارغة | التحقق من الحقول الأساسية قبل الحفظ؛ فشل التحقق يوقف اللقطة ويُبقي البيانات السابقة |
| قراءة إشعار في تبويب آخر أو إضافة إشعار جديد قد تغيّر ترقيم الصفحات وتتسبب في تجاوز سجلات | متابعة القائمة من توقيت ومعرّف آخر إشعار معروض؛ دعم الردود القديمة باقٍ أثناء النشر |
| التقارير قد تجمع معارك سجل تاريخي لا يقابله عضو في القائمة الحالية | الاقتصار على الأعضاء الذين تستخدمهم بقية إحصاءات التقرير ولوحة التحكم، في الفترة الحالية والمقارنة السابقة |
| بطاقة سجل العضوية على الهاتف تعرض العدد غير المعروف كصفر | إظهار «غير معروف»، مع الإبقاء على الصفر عندما يكون مسجّلًا فعلًا |
| اكتمال حفظ ملاحظة متأخر قد يمحو مسودة أحدث | ربط اكتمال الحفظ بالملاحظة التي أُرسلت، وحماية المسودة الجديدة ومنع تكرار طلب الحفظ الجاري |
| استجابة ناجحة شكليًا لكن محتواها ليس JSON كانت تُخزّن كبيانات فارغة | رفض الاستجابة غير الصالحة، وإبقاء القراءة قابلة لإعادة المحاولة بدل تخزينها في الذاكرة المؤقتة |
| قد تُحسب مهلة إعادة محاولة الدخول 601 ثانية عند تنافس طلبات على القفل، فتتحول رسالة الانتظار إلى خطأ | حساب المدة بعد انتظار القفل وحصرها في 1–600 ثانية، مع بقاء حد المحاولات والصلاحيات كما هما |

يخص الترحيل `202609160019_login_retry_clock.sql` حساب مهلة الدخول فقط؛ بقية إصلاحات المتابعة لا تحتاج تغيير مخطط قاعدة البيانات. لا تُعيد التغييرات كتابة السجلات التاريخية ولا تفترض قيمًا غائبة. تُختبر حالات التأخر والانقطاع والتداخل ببيانات معزولة، ويُفحص الموقع المنشور دون تغيير حالة الإشعارات أو الملاحظات الحية لأجل الاختبار.

اجتاز التحقق المحلي 275 اختبار تطبيق و120 اختبار PostgreSQL، إضافة إلى TypeScript وESLint وبناء الإنتاج. شمل اختبار المصدر البطيء ثماني دورات متتالية لنادٍ من 30 عضوًا للتأكد من وصول الدور إلى الجميع، واختبار توقف استعلامات المصدر الاحتياطي عند انتهاء ميزانيتها الزمنية. أعاد اختبار قفل محدد إنتاج خطأ 601 ثانية قبل الإصلاح، ونجح بعده. نجحت النسخة الاحتياطية السابقة للترحيل رقم `35124161815` مع اختبار الاستعادة.
