# إصلاحات مراجعة BrawlStatz

تحديث 15 سبتمبر 2026. يوثّق هذا الملف إصلاح المشكلات الخمس عشرة التي كشفتها مراجعة التطبيق، بالترقيم نفسه. اكتمل التحقق المحلي من الإصلاحات كما هو موضح أدناه. يمكن متابعة حالة النسخة المنشورة في [سجل النشر](https://github.com/M3TZ-123/brawl-club-manager/deployments).

1. **[P1] فقدان بيانات الانضمام والمغادرة أثناء المزامنة.**
   كانت دفعة الحفظ تجمع سجلات بحقول مختلفة، فتحوّل الحقول المحذوفة من بعض السجلات إلى قيم فارغة وتطمس التواريخ والعدادات القديمة. أصبحت سجلات الحفظ تحمل مجموعة الحقول نفسها مع الاحتفاظ بالقيم الموجودة، وتُترك الملاحظات خارج تحديث المزامنة. يمنع ذلك تكرار الفقد؛ لا يعيد البيانات التي مُسحت سابقًا.

2. **[P1] إعلان نجاح المزامنة رغم فشل الحفظ.**
   كانت أخطاء Supabase تُعاد داخل النتيجة دون أن توقف التنفيذ، كما كان تسجيل النشاط قد يسبق إنشاء العضو الذي يعتمد عليه. تُفحص عمليات قاعدة البيانات الآن وتُظهر الفشل، ويُحفظ الأعضاء قبل سجلات نشاطهم، ولا يُحدّث وقت نجاح المزامنة إلا بعد اكتمال العمليات المطلوبة.

3. **[P1] ثغرات معروفة في الاعتماديات.**
   كانت إصدارات Next.js وبعض الاعتماديات المباشرة وغير المباشرة مشمولة بتنبيهات أمنية، ما يعرّض التطبيق للمشكلات التي تنطبق على بيئة تشغيله. حُدّث Next.js وأدواته المتوافقة وReact وAxios والاعتماديات المتأثرة وملف القفل. لم يعد فحص `npm audit` يُظهر ثغرات معروفة في الشجرة الحالية.

4. **[P1] إغلاق معالج الإعداد قبل أول مزامنة.**
   كان حفظ مفتاح API ووسم النادي يُعامل كاكتمال للإعداد، فتختفي خطوة المزامنة وتظهر لوحة فارغة. أصبح الاكتمال يتطلب مزامنة ناجحة مؤكدة من الخادم، ويستطيع المعالج متابعة خطوة المزامنة إذا كانت بيانات الاتصال محفوظة مسبقًا. يتيح رابط «Edit Configuration» تصحيح الإعدادات إذا تعذر إكمال المزامنة.

5. **[P2] تعطل خدمة التصنيف يستهلك مهلة المزامنة.**
   كانت مهلات طلبات التصنيف الاختيارية تتكرر عبر دفعات الأعضاء، وقد تتجاوز وحدها مهلة التنفيذ. أصبحت هذه الطلبات تعمل بأربعة عمال متوازيين مستقلين عن طلبات Brawl Stars الأساسية، بمهلة مشتركة مدتها ثماني ثوانٍ. يمنع ذلك تأخر تحديث الأعضاء في نهاية القائمة بسبب بطء الطلبات الأساسية؛ تُلغى الطلبات عند انتهاء المهلة وتُحتفظ بقيم التصنيف المحفوظة عندما تكون الخدمة غير متاحة.

6. **[P2] تأخر تنبيهات الخمول بسبب إعادة احتساب الملاحظة كنشاط.**
   كانت مشاهدة المباراة القديمة في كل مزامنة تُنتج سجلًا حديثًا يُمدّد مدة النشاط مرة أخرى. أصبح التصنيف يعتمد على وقت اللعب الفعلي أو تغيّر جديد في الكؤوس، مع تطبيق حد الخمول المحفوظ، بدل اعتماد وقت إعادة مشاهدة الدليل نفسه.

7. **[P2] اختلاف حالة النشاط بين التقارير وبقية الصفحات.**
   كانت التقارير وصفحة العضو تعتمد على حالة محفوظة قديمة، وكانت التقارير تثبّت عدد قليلي النشاط عند الصفر. أصبح الحساب يستخدم قواعد النشاط المشتركة وقت الطلب، ويعرض النشط وقليل النشاط والخامل بصورة متسقة، مع استبعاد أدلة النشاط ذات التواريخ المستقبلية غير الصالحة.

8. **[P2] احتساب أيام خارج الفترة الأسبوعية المعروضة.**
   كان جمع يوم البداية كاملًا مع طرح سبعة أيام من الوقت الحالي يُدخل جزءًا إضافيًا يصل تقريبًا إلى يوم، فتختلف المجاميع عن الرسم والمؤشرات. وُحّدت الفترة لتشمل اليوم الحالي بتوقيت UTC والأيام الستة السابقة، وطُبّقت حدودها على المجاميع والتواريخ والأحداث المعروضة.

9. **[P2] تقسيم المباراة الواحدة إلى بطاقات مكررة عند تحميل المزيد.**
   كان تقسيم صفوف اللاعبين إلى صفحات يقطع أفراد الفريق بين صفحتين، فتظهر بطاقات ناقصة وتتوزع نتائجهم. أصبح الخادم يستكمل جميع الصفوف عند توقيت نهاية الصفحة قبل إعطاء موضع الصفحة التالية، وتمنع الواجهة تكرار بطاقات المباراة عند الدمج. وإذا أدت إضافة معارك جديدة إلى تداخل الصفحات، تُدمج أسماء المشاركين دون إسقاط الزملاء المعروفين.

10. **[P2] التحديث المباشر يعرض نسخة قديمة من سجل المعارك.**
    كان إشعار إدراج مباراة يعيد استخدام الذاكرة المؤقتة، فتظل المباراة الجديدة غائبة رغم ظهور حالة «مباشر». أصبحت الإشعارات المتقاربة تُجمع بتأخير قصير ثم تفرض جلب بيانات جديدة، مع منع الطلب الأقدم من استبدال نتيجة أحدث في الذاكرة المؤقتة أو الواجهة.

11. **[P2] تعذر الوصول إلى الإشعارات الأقدم المطابقة للفلاتر.**
    كانت الواجهة تجلب أحدث مئة إشعار ثم تُصفّيها محليًا، فتفقد إشعارات غير مقروءة أو من فئة مطلوبة خارج هذه المجموعة. أصبحت فلاتر القراءة والفئة تُطبّق في الخادم قبل تحديد الصفحة، وأُضيف تحميل المزيد للوصول إلى النتائج الأقدم.

12. **[P2] اختفاء الأعضاء العائدين من السجل المصفّى بالتاريخ.**
    كان الفلتر يعتمد على تاريخ أول انضمام وآخر مغادرة، فيستبعد من عاد حديثًا إذا كان التاريخان قديمين. أصبح يشمل أحداث الانضمام والمغادرة داخل الفترة، مع الاحتفاظ بالاعتماد على تواريخ السجلات القديمة عند الحاجة، وجلب جميع صفحات السجل والأحداث دون اختراع تواريخ مفقودة.

13. **[P2] حفظ الإعدادات يستبدل وقت مزامنة أحدث بقيمة قديمة.**
    كان المتصفح يرسل وقت المزامنة الموجود عنده مع أي تعديل، ويقبله الخادم حتى إن تجاوزه وقت أحدث. أُزيلت حقول متابعة المزامنة من الحفظ العادي ومن قائمة الحقول المقبولة؛ يديرها الخادم. وعند تغيير النادي، يطلب الخادم مزامنة جديدة بدل إبقاء علامة اكتمال تخص النادي السابق.

14. **[P2] سكربت استكمال بيانات المعارك لا يملك صلاحية الكتابة.**
    كان السكربت يستخدم المفتاح العام الذي تمنعه سياسات RLS من التعديل وقراءة الإعدادات، وقد يعلن تحديث صفوف لم تتغير. أصبح يتطلب `SUPABASE_SERVICE_ROLE_KEY` ويتحقق من معرّفات الصفوف المعدلة فعليًا، ويفصل الصفوف المتجاوزة عن الأخطاء. يظل وضع المعاينة هو الافتراضي، وتؤدي أخطاء الحفظ في وضع التطبيق إلى خروج بفشل.

15. **[P3] إخفاء سبب فشل التحقق من النادي.**
    كان مساعد API يحوّل الخطأ إلى رسالة عامة ويفقد رمز HTTP، فتظهر أخطاء المفتاح والنادي كخطأ داخلي عام. أصبح نوع `BrawlApiError` يحتفظ بالرمز؛ يعرض التحقق رسالة واضحة للمفتاح غير الصالح `403`، والنادي غير الموجود `404`، وتجاوز حد الطلبات `429`.

## التحقق المنجز

- `npm test`: نجحت **60 من 60** حالة اختبار، باستخدام بيانات وبدائل معزولة للخدمات.
- `npm run lint`: نجح.
- `npm run build`: نجح.
- `npx tsc --noEmit`: نجح.
- `npm audit`: **صفر ثغرات معروفة** في نتيجة الفحص الحالية.
- اختبار الإعداد في المتصفح: بقي المعالج في خطوة المزامنة بعد حفظ الإعدادات؛ أبقاه فشل مُحاكى برمز `503` مفتوحًا مع رسالة خطأ؛ استأنف الخطوة بعد إعادة التحميل، ثم فتح لوحة التحكم بعد نجاح المحاولة التالية وتأكيد علامة الاكتمال من الخادم.
- فتح رابط «Edit Configuration» من خطوة المزامنة صفحة إعدادات النادي والمفتاح بنجاح، دون أخطاء في المتصفح.
- اختبار الإشعارات في المتصفح: أظهر «تحميل المزيد» عند الإزاحة `100` الإشعارين الأقدم، ووصل فلتر غير المقروء وفئة الانضمام إليهما مباشرة عبر تصفية الخادم.
- استُخدمت بيانات وردود محاكاة لاختبارات الإعداد والإشعارات؛ لم تظهر أخطاء غير متوقعة في وحدة التحكم، وظهر فقط خطأ المزامنة المُحاكى المقصود.
- فحص قراءة فقط للبيانات المتصلة بالمشروع: تطابقت لوحة التحكم والتقرير عند **29 عضوًا و19 عضوًا نشطًا**؛ وتطابق التقرير والتحليلات عند **1,222 معركة و794 انتصارًا**. عرض المتصفح الفترة من 9 إلى 15 سبتمبر بتوقيت UTC وفئات النشاط الثلاث دون أخطاء في وحدة التحكم.

## حدود الإصلاح واستعادة البيانات

اختبارات الإصلاحات المعروضة أعلاه لم تُجرِ مزامنة حية أو عملية استكمال بيانات حية. إصلاح الحفظ يمنع فقدان البيانات لاحقًا؛ أما استعادة سجل سبق مسحه فتتطلب نسخة احتياطية موثوقة أو أحداثًا محفوظة تثبت القيم المستعادة. لا تُستنتج تواريخ الانضمام والعدادات المفقودة من افتراضات غير مثبتة.

تُظهر المزامنة أخطاء الكتابة الآن، لكن عملياتها المتعددة ليست معاملة واحدة تتراجع بالكامل عند الفشل؛ قد تكون بعض العمليات السابقة للخطأ قد اكتملت. هذا الملف يصف الإصلاحات المحلية والتحقق المنجز، بينما يبقى تقرير المراجعة الأصلي سجلًا لحالة التطبيق قبل الإصلاح.
